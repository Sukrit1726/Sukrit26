name: needs_keyword
on: push
jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test is running"
      - run: echo "test successfull"
  deploy-app:
    needs: run-test
    runs-on: ubuntu-latest
    steps:
      - run: echo "deployed succesfully"
              