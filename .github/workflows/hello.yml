name: My first workflow
on: push
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Print message
              run: echo "hello,github actions!"
            
    say-bye:
        runs-on: ubuntu-latest
        steps:
            - name: Print message
              run: echo "hello job2,github actions!"
              